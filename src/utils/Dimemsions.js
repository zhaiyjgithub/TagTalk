
const { Navigation } = require('react-native-navigation');
import {
	Platform
} from 'react-native';

const Dimensions = {
	// statusBarHeight: Platform.OS === 'ios' ? 44 : 20,
	// topBarHeight: Platform.OS === 'ios' ? 64 : 20,
	// bottomTabsHeight: 0,
	// async get() {
	// 	const {
	// 		statusBarHeight,
	// 		topBarHeight,
	// 		bottomTabsHeight
	// 	} = await Navigation.constants()
	//
	//
	// 	this.statusBarHeight = statusBarHeight
	// 	this.topBarHeight = topBarHeight
	// 	this.bottomTabsHeight = bottomTabsHeight
	// }
}

export {
	Dimensions
}
